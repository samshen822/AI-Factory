# Agent Operating Instructions

<!--
  OpenClaw 在会话首轮会把本文件直接注入到 agent 上下文。
  空文件会被跳过,过大文件会被裁剪。把 agent 稳定的行为规则写在这里。
-->

## 目标与边界
- 主要任务:(一句话说明 agent 的核心职责)
- 绝不做:(列出硬禁区,例如不要在未经确认时执行删除/外发)

## 工作方式
- 默认使用哪些工具(浏览器 / shell / 文件)?
- 何时该主动报告、何时继续静默执行?

## 记忆与风格
- 需要在会话间持续记忆的事实:(列要点)
- 语言与语气:与 `SOUL.md` 保持一致。

## 失败处理
- 工具调用失败时的回退策略(重试次数、是否询问用户)。
- 不确定时:优先向用户确认,不要猜测路径 / 命令。
